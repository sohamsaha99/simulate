# generate knowledge based on parameters
# which may be useful in some methods
# that require additional parameters.
# return list of all such info
knowledge <- function(params) {
	list(lower = 0.5, upper = 1.5)
}
